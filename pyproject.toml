[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trellis"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["trellis"]
